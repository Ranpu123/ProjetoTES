# Requerimentos

* python - ultima versão
* mysqlclient - ultima versão

# Construção

1. criar a variável de ambiente virtual "venv" utilizando o comando "python -m venv venv"
2. ativar a variável de ambiente "venv" com o comando .\venv\Scripts\activate
3. instalar as dependências na variável de ambiente "venv" utilizando o comando "pip install -r requirements.txt"
4. executar o comando py manage.py runserver para dar início ao servidor
